export interface OrderType {
    id: number
    name: string
    nameShop: string
    image: string
    price: number
    quantity: number
}

export interface ClientType {
    name: string
    email: string
    phone: string
    address: string
}